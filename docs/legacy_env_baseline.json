{
  "max_per_file": {
    "Makefile": 1
  },
  "note": "C4/B2/B16/R9.4/R9.5: refs EJECUTABLES+DOCUMENTALES a ante/bin y ante_nf/bin en el call graph. Trinquete: solo puede BAJAR. total == sum(max_per_file). Desglose via check_no_legacy_envs.current_breakdown. MIGRADO 29->1: los 8 runbooks + sync_all -> run-command (heredocs/-c a tools/{merge_campaign_pools,check_deep_refit,print_panel_summary,hash_file}.py, B66); comentarios documentales de pre-commit/dvc.yaml/Makefile depurados; mlflow-sync -> run-command. RESTA (1): `PY ?= ante/bin/python` en Makefile -- default de las ~40 recetas; su retiro exige migrar cada target a su wrapper de perfil (R9.4.1, acoplado con R9.3/dvc.yaml). Objetivo R9.5: total 0. NO borrar los venvs fisicos hasta autorizacion de cutover.",
  "total": 1
}
