# Entorno GPU (AWS A10G / g5.xlarge) para el frontier pesado de forecasting — perfil DEEP.
#
# SHIM al lock HASHEADO del perfil deep (P0R.4, ronda 10): instala el cierre transitivo EXACTO,
# reproducible y AUDITADO (torch 2.12.1+cu126 desde el índice oficial PyTorch CUDA 12.6, con
# verificación de hashes). Es la MISMA receta que corre local en `ante_nf`, de modo que la receta
# ganadora (global + diferencia + local_scaler + HPO Optuna) es idéntica en GPU.
#
#   pip install -r aws_gpu/requirements.txt   # desde la raíz del repo, en la instancia EC2
# El lock lleva un sha256 por pin, así que pip entra automáticamente en modo --require-hashes
# (todo requirement debe traer hash o la instalación aborta) — verificación de integridad de wheels.
#
# La fuente directa del perfil está en requirements/deep.in (+ wrapper deep-linux-cu126.in);
# regenerar con `bash tools/make_locks.sh` (make lock) — nunca editar el lock a mano.
# NOTA P0R.4: este perfil RESUELVE+audita+hashea el cierre CUDA; la EJECUCIÓN GPU real en A10G
# se certifica en P0R.5. Chronos zero-shot / fine-tuning (LoRA) opcional: chronos-forecasting y
# transformers ya vienen en el lock; ver aws_gpu/chronos_lora.py. Mamba/uni2ts/timesfm siguen
# fuera del perfil (cada uno requiere su propio entorno CUDA).
-r ../locks/deep-linux-x86_64-cu126.txt
